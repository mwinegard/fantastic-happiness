{
  "name": "uno-web-game",
  "version": "1.0.0",
  "description": "Multiplayer UNO on Node + Socket.io",
  "main": "server.js",          // <-- This MUST match the filename that starts your server
  "scripts": {
    "start": "node server.js"   // <-- Render runs this. Locally you’ll run `npm start`
  },
  "dependencies": {
    "express": "^4.18.2",       // Web-server framework
    "socket.io": "^4.7.2"       // Real-time engine
  }
}
